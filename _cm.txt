fix(m6b1): 修正注释与事实不符——落盘链路并不喂 check_rhythm

问题（纪律 #7 违反）
--------------------
M6-B1 新增的三处注释声称：「tension_curve 零生产调用点 ⇒
check_rhythm 这类跨章张力分析永远没有输入」，暗示本 hook 接线后
check_rhythm 就有输入了。**这是错的**：

  · 本 hook → 写**文件台账** TENSION_LEDGER；读方是 read_tension()
  · check_rhythm → 读**内存 state** self._scores
  · 而 _scores 只由 evaluate_chapter() 填充，该入口**仍是零生产调用点**

⇒ 两条通道**并不相通**。原注释等于「给缺陷做伪装」（纪律 #7：
凡写「供 XX 消费」的注释，必须同时证明该消费者存在）。

取证
----
- evaluate_chapter 模块外调用点：仅 tests/（test_m6_tension 3 + test_v1_0 5）
- check_rhythm 模块外调用点：0（仅注释中出现）
- _scores 填充路径：仅 evaluate_chapter 一处

修改
----
- m5_persist._record_tension docstring：改为准确描述「只写台账 + 与
  check_rhythm 不相通」，并显式标注本条为纪律 #7 复核修正
- agentic_write.py:1604 行内注释：去掉「⇒ check_rhythm 永无输入」的误导
- tension_curve.py M6-B1 注释块：补「两条通道不相通」的边界说明，
  并加纪律 #7 条「不写供 XX 消费的注释（消费者不存在就不写）」

★★ 这条修复同时把 C 项取证结论固化进代码注释（防后续再被误读为「已接线」）。

验证
----
pytest tests/test_m6_tension.py tests/architecture → 212 passed（21 + 191）
