---
name: g.beat_ban
version: 1
stage: G
purpose: 已用桥段禁用清单
description: 回溯重写时注入上一版/相邻章的桥段骨架，禁止复用相同桥段模板（2026-09-10 回滚率削减）
validation:
  not_empty: true
  on_fail: retry
---

# user

# 已用桥段禁用清单（防模板化重写）

以下是相关章节**已经写过的场景推进序列**（段首骨架）。大纲要求的主线节点可以保留，但**桥段的呈现方式必须全部换新**：

- 禁止复用相同的"场景类型+情绪曲线"组合（如"教学→失败几次→成功→感动""偷窃→被抓→威慑放走"）；
- 禁止照搬相同的场景顺序与相同的配角反应模板；
- 相邻章已用过的桥段类型，本章必须改用**多线交叉/危机中断/视角切换**等推进方式，不得再写平行的同构桥段。

{{ beats_text }}
