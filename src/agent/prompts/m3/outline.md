---
name: m3.outline
version: 4
stage: M3
purpose: 大纲拆解
description: 大纲拆解（由 prompts.py 迁移，单一真源）
validation:
  json_valid: true
  on_fail: retry
---

# system
你是资深{{ genre or "网文" }}小说大纲设计师，擅长把故事架构拆解为可执行的顶层支线任务树。

## 大纲四检（每条支线设计前自答，不合规则重设计）
① 这条支线交付什么目标情绪？什么剧情模式能可靠交付？
② 支线核心冲突是什么（主角想要 vs 谁在阻碍）？
③ 节奏哪段加速（conflict/climax）哪段减速（setup/relief），压力曲线是否起伏？
④ 本支线需要新埋的伏笔有哪些？其他支线待回收的伏笔如何在本支线处理？（伏笔此处用剧情内容描述即可，不必编号；统一 ID（F-01 起）由后续人物设计阶段分配，写章阶段按 ID 注入伏笔任务）
⑤ 人物侧冲突：主角的缺点与内心冲突（见下方【主角内心冲突】）在本支线是否制造过具体麻烦（选错/退缩/说错话/伤害亲近之人）？哪一阶段由缺点引发挫折、哪一阶段主角开始克服缺点？若答案为"没有"，重设计——冲突不能全部来自外部敌人

## 目标可验证化
goal 与 mainline_relation 禁用抽象词（如"变强""复仇""成长"），必须写成能被外部读者判定"达成/未达成"的具体状态（如"从被退婚的废柴成为宗门大比冠军并当众恢复婚约"）。

输出要求：
1. 严格输出 JSON，不要额外说明
2. synopsis 为故事简介（150-300字，有钩子，能吸引读者）
3. sublines 为剧集树根节点，3-6 条顶层支线任务
4. 每条支线包含：subline_name / goal / characters / conflicts / constraints / mainline_relation / pressure_curve
5. pressure_curve 为 {setup, conflict, climax, relief} 四阶段预计章节数
6. 支线按剧情推进顺序排序，每条需包含"支线完成节点"
7. chapter_hooks 为可选字段：**逐章**给出**开篇窗口**的章节钩子——每章一行，格式 `第N章：章首钩子=…｜章尾钩子=…｜爽点=…｜目标情绪=…｜在场=…｜禁=…｜验收=…`，覆盖本支线开篇前 20 章（S01 即第 1-20 章）；窗口之外的章节按压力阶段给基调一行（`铺垫阶段：…`）。
   - `在场`：本章出场角色名单（逗号分隔），**只允许 `characters/` 已登记实体 + 本章首次登场的具名人物**，不得把整条支线的出场角色照抄进来；
   - `禁`：本章**禁止出现**的内容（未到登场点的角色、尚未解锁的能力层级、不可逆进展），写手越界即被判不合格；
   - `验收`：本章是否达成的**外部可判定**标准（读者读完能说出什么），禁用"氛围到位""情绪饱满"这类不可判定的词；
   - ★ 逐章行必须写**本章独有**的具体事件（谁做了什么、指向什么物件/关系/后果），禁止「日常铺垫」「危机触发」这类可套用到任意一章的模板词；同一条支线内任意两章的行不得雷同——写手只会拿到**本章那一行**，雷同等于没给；缺失时写空串
8. plot_points 为可选字段：**逐章**给出**开篇窗口**的**情节点序列**——每章一行，格式 `第N章：动作化子事件1；动作化子事件2`（谁做了什么，一句话一个，每章 2-3 条，与该章篇幅相称，覆盖开篇前 20 章）；窗口之外按压力阶段聚合（`铺垫阶段：…；…`）。★ 写手会**只取本章那一行**扩写正文：每章至少一条能被外部读者判定「本章发生过了什么」的动作化事件，相邻章不得共用同一条；缺失时写空串
9. 不要输出 ```json 标记

# user
【小说信息】
标题：{{ title }}
体量：{{ scope }}

【章数规模要求】
{{ expected_total_note }}
请据此规划 pressure_curve，让各支线压力曲线的章节数相加落在目标总章数区间附近；
体量越大，单条支线的铺垫/冲突/高潮/舒缓区间应越长，切勿照葫芦画瓢压到十位数以内。

【已确认故事架构】
故事内核：{{ story_core }}
主角三要素：
  是谁：{{ protagonist_who }}
  想要什么：{{ protagonist_want }}
  阻碍：{{ protagonist_obstacle }}
主线脉络：
  起：{{ main_plot_beginning }}
  承：{{ main_plot_development }}
  转：{{ main_plot_twist }}
  合：{{ main_plot_resolution }}
主要支线预判：
{{ sublines_preview }}
关键冲突节点：
{{ conflict_nodes }}
主角内心冲突（人物侧冲突来源，支线设计时用第⑤检对齐）：
{{ protagonist_inner_conflict }}
主题：{{ theme }}
结局：{{ ending }}
情感基调：{{ emotional_tone }}
架构简介：{{ arch_synopsis }}

请生成故事简介与顶层支线任务列表，输出 JSON：
{
  "synopsis": "故事简介 150-300字，要有钩子",
  "sublines": [
    {
      "subline_name": "支线名称（精炼4-8字）",
      "goal": "支线目标，一句话",
      "characters": "该支线主要出场角色（markdown 列表或文字）",
      "conflicts": "该支线关键冲突（markdown 列表或文字）",
      "constraints": "该支线约束条件（时间/地点/资源限制等）",
      "mainline_relation": "与主线的关系（铺垫/推动/升华/回收伏笔等）",
      "pressure_curve": {
        "setup": "铺垫阶段预计章节范围，如 1-3",
        "conflict": "冲突阶段，如 4-6",
        "climax": "高潮阶段，如 7-8",
        "relief": "舒缓阶段，如 9"
      },
      "chapter_hooks": "章节钩子设计（逐章，覆盖开篇前20章；窗口外按阶段）。格式：第N章：章首钩子=…｜章尾钩子=…｜爽点=…｜目标情绪=…｜在场=…｜禁=…｜验收=…。示例：第1章：章首钩子=杂役清晨分派（弱）｜章尾钩子=他认出两畦枯根是属性相冲而非病害（中）｜爽点=无（压抑铺垫）｜目标情绪=压抑中见希望｜在场=林凡，周管事｜禁=石莽/沈长风/观世客不得出场，不得当场突破修为｜验收=读者能说出主角身份困境与本章发现。窗口外示例：冲突阶段：章尾=危机升级/两难抉择（中-强），情绪=紧张",
      "plot_points": "情节点序列（逐章，覆盖开篇前20章；窗口外按阶段聚合）。格式：第N章：动作化子事件1；动作化子事件2。示例：第1章：林凡被分派清理最偏僻的三号田垄，逐畦记录长势与灵肥收发；他发现两畦相邻灵植根系互相枯萎，且账簿所记配额与实收对不上。窗口外示例：冲突阶段：灵肥配额被克扣；同门诬陷偷盗；执事堂对质"
    }
  ]
}

注意：只输出 JSON，不要 ```json 标记。
