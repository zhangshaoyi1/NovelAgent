{# 注意：本模板只输出正文；frontmatter 由 SettingManager.save_world 统一写入（元数据以 metadata dict 为准），在此重复输出会导致双 frontmatter。 #}
# 总设定集 · {{ title }}

## 故事简介

{{ synopsis }}

## 风格配置

| 项 | 值 |
|---|---|
| 文风 | {{ style.tone }} |
| 视角 | {{ style.pov }} |
| 节奏 | {{ style.rhythm }} |
| 章节字数 | {{ style.chapter_length }} |
| 信息密度 | {{ style.info_density }} |
| 禁用元素 | {{ style.banned_elements | join(", ") }} |

## 世界观

{{ worldview }}

## 力量体系（冻结）

{{ power_system }}

## 境界体系（冻结）

{{ realm_system }}

## 主要势力

{{ factions }}

## 金手指登记

{{ golden_finger }}

## 风格与禁用规则

- 禁止说明文式设定堆砌
- 禁用词限量：突然/忽然/就在这时/微微一笑 全章 ≤ 2 次
- 场景+动作+环境描写合计 ≥ 30%
