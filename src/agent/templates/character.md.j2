---
name: "{{ name }}"
role: "{{ role }}"
faction: "{{ faction }}"
realm: "{{ realm }}"
first_appearance: "{{ first_appearance }}"
created_at: "{{ created_at }}"
---

# 角色档案 · {{ name }}

## 基础

- 身份：{{ identity }}
- 势力：{{ faction }}
- 境界：{{ realm }}
- 出场支线：{{ first_appearance }}

## 内核

- **核心动机**：{{ core_motivation }}
- **表层目标**：{{ surface_goal }}
- **深层目标**：{{ deep_goal }}
- **秘密**：{{ secret }}
- **弧光**：
  - 起始状态：{{ arc.start }}
  - 终结状态：{{ arc.end }}

## 语言指纹

- **口头禅**：{{ language_fingerprint.catchphrase }}
- **句式偏好**：{{ language_fingerprint.sentence_style }}
- **用词习惯**：{{ language_fingerprint.vocabulary }}
- **禁用词**：{{ language_fingerprint.banned_words | join(", ") }}

## 关系

{{ relations }}

## 校验规则

{% if role == "antagonist" %}
- **反派动机合理性自检**：{{ validation.motivation_check }}
{% endif %}
{% if role == "supporting" %}
- **配角独立支线钩子**：每 {{ validation.appearance_interval }} 章至少露面一次推进自己的事
{% endif %}
