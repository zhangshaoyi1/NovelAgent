---
updated_at: "{{ updated_at }}"
current_chapter: "{{ current_chapter }}"
---

# 初始角色关系网

## Mermaid 可视化

```mermaid
graph LR
{% for e in edges %}    {{ e.from }}(["{{ e.from }}·{{ lookup_label(e.from) }}"]) -->|{{ e.type }}(强度{{ e.intensity }})| {{ e.to }}(["{{ e.to }}·{{ lookup_label(e.to) }}"])
{% endfor %}
```

## 图例

| 分组 | 含义 |
|---|---|
| protagonist | 主角 |
| antagonist | 反派 |
| mentor | 导师 |
| supporting | 配角 |

## 节点

| ID | 角色 | 分组 |
|---|---|---|
{% for n in nodes %}| {{ n.id }} | {{ n.label }} | {{ n.group }} |
{% endfor %}

## 边（关系）

| 起 | 止 | 类型 | 强度 | 起于 | 备注 |
|---|---|---|---|---|---|
{% for e in edges %}| {{ e.from }} | {{ e.to }} | {{ e.type }} | {{ e.intensity }} | {{ e.since }} | {{ e.note }} |
{% endfor %}
