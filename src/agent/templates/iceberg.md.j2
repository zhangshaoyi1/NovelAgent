# 建书冰山 · {{ title }}

> 水面之上是读者看到的故事；水面之下，是一个在动笔之前就已完整运转的世界。
> 角色看不到、还没登场，不代表事件没有发生。先造好这座冰山，再让文字从中生长。

{% for g in groups %}
## {{ g.label }}（{{ g.primary_count }} 一级维度 · {{ g.field_count }} 字段）

_{{ g.metaphor }}_

{% for d in g.dimensions %}
### {{ d.name }}

{% for f in d.fields %}- [ ] **{{ f.name }}**：{{ f.hint }}
{% endfor %}
{% endfor %}
{% endfor %}

---

_由 NovelAgent `iceberg` 命令生成 · 共 {{ total }} 个设定字段。勾选并填充后，世界即先于文字而存在。_
