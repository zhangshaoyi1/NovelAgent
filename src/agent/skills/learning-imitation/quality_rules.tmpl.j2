技法资产质量规则 + 模板 + 速查表 + 自检清单 结构（借用 Jinja2 渲染）。

## 质量门控
- 每条技法必须以「可执行动作」表达，拒绝抽象形容与空话。
- 共性须有样本数佐证（`occurrences >= 2` 才算共性，否则标为变体）。
- 六槽位一致：产出自同一批样本的技法需能互相印证，不相互矛盾。

## 速查表
| 槽位 | 是否可直接套用 | 判定 |
|------|--------------|------|
| gimmick | 是（一句话） | 是否一句话可复述卖点 |
| character | 是（标签集） | 是否有可落地的对话/动作标签 |
| pacing | 是（节奏曲线） | 是否给出钩子密度/情绪走向 |
| intro | 是（前千字） | 是否给出捕获序列 |
| plot_refine | 深化素材 | 是否含可复用算子 |
| draft_excerpt | 深化素材 | 是否带标注示例 |

## 自检清单
- [ ] 每条规则是否可执行（能写出「当 X → 做 Y」）？
- [ ] 共性是否 ≥ 2 篇，单篇是否已标「变体」？
- [ ] 六槽位是否完整、互相印证？
- [ ] 资产是否先落预览区、确认后才入 library？